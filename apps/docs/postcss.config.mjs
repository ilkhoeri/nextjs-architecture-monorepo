/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // "tailwindcss/nesting": "postcss-nesting",
    // autoprefixer: {},
    "@tailwindcss/postcss": {}
  }
};

export default config;
